# path utilizzato per la creazione del socket AF_UNIX
UnixPath = /tmp/chatty_socket

# numero massimo di connessioni concorrenti gestite dal 
serverMaxConnections  = 32

# numero di thread nel 
poolThreadsInPool    = 8

# dimensione massima di un messaggio testuale (numero di caratteri)
MaxMsgSize       = 51

2# dimensione massima di un file accettato dal server (kilobytes)
MaxFileSize      = 1024

# numero massimo di messaggi che il server 'ricorda' per ogni 
clientMaxHistMsgs      = 16

# directory dove memorizzare i files da inviare agli utenti
DirName          = /tmp/chatty

# file nel quale verranno scritte le statistiche
StatFileName     = /tmp/chatty_stats.txt